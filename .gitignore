# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.ipynb_checkpoints/

# Tamarin / ProVerif build artifacts
*.aux
formal/**/output/
*.tamarin-proof
*.spthy.proof

# liboqs build
provenance/crypto/_build/
*.so
*.dylib

# Home Assistant runtime
actuation/homeassistant/.storage/
actuation/homeassistant/*.log
actuation/homeassistant/.cloud/
actuation/homeassistant/deps/

# Local env / secrets
.env
env/versions.local.lock

# Scratch
scratch/*
!scratch/.gitkeep

# OS
.DS_Store
_apalache-out/
x-*.tla
*.itf.json

# Manuscript build artifacts (source is tracked; PDFs are tracked deliberately)
manuscripts/*.aux
manuscripts/*.log
manuscripts/*.blg
manuscripts/*.bbl
manuscripts/*.out
manuscripts/sections/*.log
texput.log

# Pre-submission review notes: internal working documents, not part of the artifact
manuscripts/feedback/

# Manuscript sources are deliberately not published with this artifact.
manuscripts/
